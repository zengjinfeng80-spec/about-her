# 暂停内置 AI 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让“雪梨”只保存和同步原始记录，不显示 AI 入口，也不创建或调用任何 AI 分析任务。

**Architecture:** 保留现有数据库、Edge Functions 和未来可恢复的服务接口，只在客户端保存流程中停止 AI 调度，并从 React 界面移除 AI 专属视图与状态。新记录统一写入 `analysis_status: 'idle'`，媒体上传、登录、同步和删除流程不变。

**Tech Stack:** React 19、TypeScript、Supabase JS、Vitest、Testing Library、Vite、GitHub Pages

## Global Constraints

- Gemini 实现分支 `codex/gemini-free-tier` 不合并、不推送、不部署。
- 不创建、保存或部署任何模型 API Key。
- 不修改数据库结构、RLS、邮箱验证码登录或云端媒体存储。
- 图片和录音只保存原件，不做理解、转写或内容提取。
- 只修改与暂停 AI 直接相关的代码、测试、样式和说明。

---

### Task 1: 停止保存流程中的 AI 调度

**Files:**
- Modify: `src/cloud/service.test.ts`
- Modify: `src/cloud/service.ts`

**Interfaces:**
- Consumes: `SupabaseMemoryService.createEntry(input: CreateEntryInput): Promise<MemoryEntry>`
- Produces: 新记录写入 `analysis_status: 'idle'`；保存流程不访问 `analysis_jobs`，不调用 `analyze-entry`

- [ ] **Step 1: 写入失败测试**

在 `src/cloud/service.test.ts` 把 import 改为 `import { describe, expect, it, vi } from 'vitest'` 和 `import { buildStoragePath, parseAskResponse, SupabaseMemoryService } from './service'`，再增加一个最小 Supabase 客户端桩，调用无附件的 `createEntry`，并断言：

```ts
it('保存记录时保持 idle 且不调度 AI', async () => {
  const insertEntry = vi.fn().mockResolvedValue({ error: null });
  const invoke = vi.fn();
  const from = vi.fn((table: string) => {
    if (table === 'entries') return {
      insert: insertEntry,
      select: vi.fn().mockReturnValue({
        order: vi.fn().mockResolvedValue({
          data: [{
            id: '00000000-0000-4000-8000-000000000001', content: '原始记录', happened_at: '2026-08-16T08:00:00.000Z',
            created_at: '2026-08-16T08:00:01.000Z', revision: 1,
            analysis_status: 'idle', analysis_error: null,
          }],
          error: null,
        }),
      }),
    };
    if (table === 'profiles') return { select: vi.fn().mockReturnValue({ maybeSingle: vi.fn().mockResolvedValue({ data: null, error: null }) }) };
    if (table === 'attachments') return { select: vi.fn().mockResolvedValue({ data: [], error: null }) };
    if (table === 'claims') return { select: vi.fn().mockReturnValue({ order: vi.fn().mockResolvedValue({ data: [], error: null }) }) };
    if (table === 'claim_evidence') return { select: vi.fn().mockResolvedValue({ data: [], error: null }) };
    throw new Error(`不应访问数据表：${table}`);
  });
  const client = {
    auth: { getUser: vi.fn().mockResolvedValue({ data: { user: { id: 'user-1' } }, error: null }) },
    from,
    functions: { invoke },
    storage: { from: vi.fn() },
  };
  vi.spyOn(crypto, 'randomUUID').mockReturnValue('00000000-0000-4000-8000-000000000001');

  const service = new SupabaseMemoryService(client as never);
  const result = await service.createEntry({ content: ' 原始记录 ', happenedAt: '2026-08-16T08:00:00.000Z', files: [] });

  expect(insertEntry).toHaveBeenCalledWith(expect.objectContaining({ analysis_status: 'idle' }));
  expect(from).not.toHaveBeenCalledWith('analysis_jobs');
  expect(invoke).not.toHaveBeenCalled();
  expect(result.analysisStatus).toBe('idle');
});
```

- [ ] **Step 2: 运行测试并确认先失败**

Run: `npm test -- src/cloud/service.test.ts`

Expected: FAIL，因为当前实现写入 `queued` 并访问 `analysis_jobs`。

- [ ] **Step 3: 实现最小保存流程**

在 `src/cloud/service.ts` 中：

```ts
const { error: entryError } = await this.client.from('entries').insert({
  id: entryId, user_id: userId, content: input.content.trim(), happened_at: input.happenedAt,
  revision: 1, analysis_status: 'idle',
});
```

保留附件上传循环，删除其后的 `analysis_jobs` upsert、`analyze-entry` invoke 以及把分析错误写回记录的 catch 分支。附件上传或附件元数据写入失败仍直接抛出原有错误。

- [ ] **Step 4: 运行测试确认通过**

Run: `npm test -- src/cloud/service.test.ts`

Expected: PASS，且测试明确证明没有 AI 调度。

- [ ] **Step 5: 提交**

```bash
git add src/cloud/service.ts src/cloud/service.test.ts
git commit -m "feat: pause automatic AI analysis"
```

### Task 2: 移除 AI 页面入口与记录状态

**Files:**
- Modify: `src/app/App.test.tsx`
- Modify: `src/app/App.tsx`
- Modify: `src/app/components.tsx`
- Modify: `src/styles.css`

**Interfaces:**
- Consumes: `App`、`EntryRow`、现有三项主导航
- Produces: 仅“档案、记录、回忆”三项导航；记录卡片不呈现分析状态、错误或重试操作

- [ ] **Step 1: 更新界面测试并确认失败**

将本地新增记录测试改为断言保存后只显示原文和“记录已保存”，并加入：

```ts
it('隐藏问记录和全部分析状态入口', () => {
  render(<App initialSnapshot={{
    ...snapshot,
    entries: [{ ...snapshot.entries[0], analysisStatus: 'failed', analysisError: '自动分析暂时失败' }],
  }} persist={false} />);

  expect(screen.queryByRole('button', { name: '问记录' })).not.toBeInTheDocument();
  expect(screen.queryByText('分析失败')).not.toBeInTheDocument();
  expect(screen.queryByText('自动分析暂时失败')).not.toBeInTheDocument();
  expect(screen.queryByRole('button', { name: '重试' })).not.toBeInTheDocument();
});
```

云端保存测试继续验证 `createEntry` 和删除，但不再期待显示 `analysisError`。运行：

Run: `npm test -- src/app/App.test.tsx`

Expected: FAIL，因为当前界面仍显示“问记录”和分析状态。

- [ ] **Step 2: 移除问记录视图**

在 `src/app/App.tsx` 中：

- 从图标 import 移除 `MessageCircleQuestion`。
- 从服务类型 import 移除 `MemoryAnswer`。
- 将 `View` 改为 `'profile' | 'capture' | 'timeline' | 'settings' | 'review'`。
- 删除 `view === 'ask'` 的渲染、底部“问记录”按钮和整个 `AskPage`。
- 本地记录的 `analysisStatus` 改为 `idle`。
- 保存前使用 `setMessage(service ? '正在安全保存…' : '')`，成功后统一使用 `setMessage('记录已保存')`。
- `CapturePage` 调用 `EntryRow` 时不再传入 `onRetry`。

- [ ] **Step 3: 简化记录卡片**

将 `src/app/components.tsx` 的 `EntryRow` 完整改为：

```tsx
export function EntryRow({ entry, onDelete }: { entry: MemoryEntry; onDelete?: () => Promise<void> }) {
  const [confirmingDelete, setConfirmingDelete] = useState(false);
  const [message, setMessage] = useState('');
  return (
    <article className="entry-row">
      <div className="entry-time"><Clock3 size={14} /><time>{new Date(entry.happenedAt).toLocaleString('zh-CN', { month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit' })}</time></div>
      {entry.content && <p>{entry.content}</p>}
      {entry.attachments.length > 0 && <div className="attachment-summary">
        {entry.attachments.some((item) => item.kind === 'image') && <span><Image size={14} />图片</span>}
        {entry.attachments.some((item) => item.kind === 'audio') && <span><Mic size={14} />语音</span>}
      </div>}
      {entry.attachments.length > 0 && <div className="entry-media">
        {entry.attachments.map((attachment) => attachment.kind === 'image' && attachment.url
          ? <img key={attachment.id} src={attachment.url} alt={attachment.name} />
          : attachment.kind === 'audio' && attachment.url
            ? <div className="audio-attachment" key={attachment.id}><audio controls preload="metadata" src={attachment.url} />{attachment.transcript && <p>{attachment.transcript}</p>}</div>
            : null)}
      </div>}
      {onDelete && <div className="entry-footer"><button className="entry-action danger" aria-label="删除这条记录" onClick={() => setConfirmingDelete(true)}><Trash2 size={14} />删除</button></div>}
      {confirmingDelete && <div className="entry-delete-confirm"><span>删除后，依赖这条记录的档案结论也会失效。</span><button className="secondary-button" onClick={() => setConfirmingDelete(false)}>取消</button><button className="danger-button" aria-label="确认删除记录" onClick={() => void onDelete?.().catch((error: unknown) => setMessage(error instanceof Error ? error.message : '删除失败'))}>确认删除</button></div>}
      {message && <small className="entry-message" role="status">{message}</small>}
    </article>
  );
}
```

同时删除 `RotateCcw`、`Sparkles` import、分析状态映射、分析错误文本和重试消息逻辑。

- [ ] **Step 4: 调整导航样式并清理失效样式**

在 `src/styles.css` 中将：

```css
.bottom-nav { grid-template-columns: repeat(3, 1fr); }
```

删除不再使用的 `.analysis-state`、`.analysis-state.failed`、`.analysis-error`、`.ask-*` 和 `.answer-*` 规则，保留 `.entry-footer` 供删除按钮布局使用。

- [ ] **Step 5: 运行界面测试确认通过**

Run: `npm test -- src/app/App.test.tsx`

Expected: PASS，问记录、状态、错误和重试均不可见。

- [ ] **Step 6: 提交**

```bash
git add src/app/App.tsx src/app/App.test.tsx src/app/components.tsx src/styles.css
git commit -m "feat: hide paused AI features"
```

### Task 3: 更新说明并完成整体验收

**Files:**
- Modify: `README.md`

**Interfaces:**
- Consumes: 已暂停的客户端行为
- Produces: 与线上实际能力一致的部署和隐私说明

- [ ] **Step 1: 更新 README**

将开头说明改为：

```md
一个手机优先的私密记忆 PWA。文字、图片和语音原件会保留并在同一邮箱账号下同步；当前版本暂不启用内置 AI。
```

把模型配置与 Edge Functions 部署段标记为“未来重新启用 AI 时使用”，并将末尾状态改为：

```md
当前版本已在客户端暂停内置 AI：新记录只保存和同步原始文字、图片与录音，不创建分析任务，也不显示“问记录”、分析状态或重试入口。服务端 AI 结构仅为后续开发保留，当前未配置模型密钥。
```

- [ ] **Step 2: 运行完整自动化验证**

```bash
npm test
npm run typecheck
npm run build
```

Expected: 所有测试通过，TypeScript 无错误，Vite 成功生成 `dist/`。

- [ ] **Step 3: 本地浏览器验收**

运行 `npm run dev -- --host 127.0.0.1`，在桌面和 `390x844` 手机视口检查：

- 三项底部导航宽度均匀，无横向溢出或重叠。
- 页面无“问记录”、分析状态、分析错误或重试按钮。
- 新建文字记录后只显示“记录已保存”，记录出现在列表中。
- 浏览器 Console 无错误，保存动作无 `analyze-entry` 或 `ask-memory` 请求。

- [ ] **Step 4: 提交说明**

```bash
git add README.md
git commit -m "docs: document paused AI features"
```

- [ ] **Step 5: 合并、推送并验证部署**

将实现分支快进合并到 `main`，推送 `origin/main`。等待 GitHub Pages 工作流成功，然后检查生产地址 `https://zengjinfeng80-spec.github.io/about-her/` 返回 HTTP 200，并在真实桌面及手机视口复核三项导航和无 AI 入口。
